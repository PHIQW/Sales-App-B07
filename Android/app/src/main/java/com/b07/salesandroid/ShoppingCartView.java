package com.b07.salesandroid;

import android.os.Bundle;
import android.widget.Button;
import android.widget.SimpleCursorAdapter;
import android.widget.TextView;


import androidx.appcompat.app.AppCompatActivity;

import com.b07.inventory.Item;

public class ShoppingCartView extends AppCompatActivity{

  //private TextView shoppingCartContentTextview;
  @Override
  protected void onCreate(Bundle savedInstanceState) {
    super.onCreate(savedInstanceState);
    //shoppingCartContentTextview = findViewById(R.id.shoppingCartContentTextview);
    //String cart = "";
    //for (Item item : account.getCart().getItems()) {
      //cart = cart + "%s (%d): %s each\n" + item.getName() + account.getCart().getAmount(item) + item.getPrice().toString();
    //}
    //shoppingCartContentTextview.setText(cart);
    //DatabaseDriverAndroid db = new com.b07.database.DatabaseDriverAndroid(this);

    //setTitle("Shopping Cart");
    //setContentView(R.layout.shopping_cart);
  }
}
